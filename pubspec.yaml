name: khmer_gift_collection
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # Backend & Auth (Developer 3)
  supabase_flutter: ^2.6.0 # Double check latest version on pub.dev

  # State Management (Developer 1 & 3)
  flutter_riverpod: ^2.5.1

  # Maps & Location (Developer 2)
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.0

  # Custom Layouts & Icons
  flutter_svg: ^2.0.10
  intl: ^0.19.0 # For local currency/date formatting

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

